import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  main: {
    flex: 1
  },

  container: {
    width: '90%',
    backgroundColor: '#ddd',
    borderRadius: 50,
    alignSelf: 'center',
    alignItems: 'center',
    marginTop: 20,
  },

  shuffleIcon: {
    alignSelf: 'center',
    position: 'absolute',
    bottom: 50,
  },
})

export default styles